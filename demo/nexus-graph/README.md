# Trusted Nexus Demo Graph

This directory is generated by the genuine NexusOS Semantic crawler from `../saas` at `http://127.0.0.1:7312`.

Regenerate it with:

```powershell
powershell -ExecutionPolicy Bypass -File scripts/prepare-nexus-graph.ps1
```

The graph is committed as a deterministic demo input so the recorded Alexa experience starts quickly and uses the exact semantic nodes, capabilities, security tiers, visual structure, interaction states, and W3C DTCG tokens produced by Nexus. It is not hand-authored Alexa metadata.

Generation evidence for this snapshot: 7 pages crawled; each page completed structural, visual, state, behavior, and discovery extraction; 22 design tokens were captured; `#theme-toggle` and `#btn-sensitive-signout` capabilities are present with `EXECUTE` and `CONFIRM` security tiers respectively.
